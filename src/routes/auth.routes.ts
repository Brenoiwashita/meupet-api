import {Router} from 'express'; import {OAuth2Client} from 'google-auth-library'; import jwt from 'jsonwebtoken'; import {User} from '../models/User'; import {auth} from '../middleware/auth';
const r=Router(); const google=new OAuth2Client();
r.post('/google',async(req,res)=>{const {idToken}=req.body; if(!idToken)return res.status(400).json({message:'idToken obrigatório'}); const ticket=await google.verifyIdToken({idToken,audience:process.env.GOOGLE_CLIENT_IDS?.split(',').map(x=>x.trim())}); const p=ticket.getPayload(); if(!p?.sub||!p.email)return res.status(401).json({message:'Conta Google inválida'}); const user=await User.findOneAndUpdate({googleSub:p.sub},{$set:{email:p.email,name:p.name,picture:p.picture}},{upsert:true,new:true,setDefaultsOnInsert:true}); const token=jwt.sign({sub:String(user._id),email:user.email,name:user.name,picture:user.picture},process.env.JWT_SECRET!,{expiresIn:'30d'}); res.json({token,user});});
r.get('/me',auth,async(req,res)=>{const user=await User.findById(req.user!.id).lean();res.json(user)}); export default r;
